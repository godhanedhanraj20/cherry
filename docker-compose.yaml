services:
  stremioplay:
    build: .
    container_name: stremioplay  
    ports:
      - "8000:8000"
    volumes:
      - ./config.env:/app/config.env
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "1m"     
        max-file: "3"      